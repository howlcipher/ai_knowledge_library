# Centralized Configuration Loader
database:
  chroma_db_path: ".chroma"
  mode: "sqlite" # 'sqlite' or 'pgvector'
  # Set pgvector_dsn in .env as DATABASE__PGVECTOR_DSN

server:
  host: "0.0.0.0"
  port: 8000
  # Set webhook_secret in .env as SERVER__WEBHOOK_SECRET

agents:
  default_language: "en_US"
  max_context_tokens: 8192

# Tiered 3 pass JSON payload pipeline (documentation/multi_agent_payload_protocol.md).
# When enabled, run_loop routes through the validation gated tier agents instead
# of the free text researcher/QA loop. Also switchable per run with --payload.
payload_pipeline:
  enabled: false
  max_attempts: 3
  artifact_dir: "logs/payloads"
  # Fail fast before pass 1: verify each tier's provider is reachable, the
  # Ollama tag exists, and a one token generation succeeds. The timeout is
  # per model and generous so a large local model can cold load.
  preflight: true
  preflight_timeout: 120.0
  # Per tier model overrides. Empty string falls back to the top level llm_model.
  # Tier 1 judges deserve the strongest model; Tier 3 drafting can run cheap and fast.
  tier_models:
    tier_1: ""  # e.g. "anthropic/claude-fable-5"
    tier_2: ""  # e.g. "anthropic/claude-sonnet-5"
    tier_3: ""  # e.g. "gemini/gemini-1.5-flash"

skill_router:
  enabled: true
  top_k: 3
  score_threshold: 0.0
  max_context_chars: 12000
  skills_dir: ".agents/skills"

active_mcps:
  - memory
  - fetch

mcp_servers:
  github:
    command: "npx"
    args:
      - "-y"
      - "@modelcontextprotocol/server-github"
    env:
      GITHUB_PERSONAL_ACCESS_TOKEN: ""  # Add token here or via .env

  sqlite:
    command: "npx"
    args:
      - "-y"
      - "@modelcontextprotocol/server-sqlite"
      - "--db-path"
      - "test.db"

  fetch:
    command: "uvx"
    args:
      - "mcp-server-fetch"

  discord:
    command: "npx"
    args:
      - "-y"
      - "@modelcontextprotocol/server-discord"
    env:
      DISCORD_BOT_TOKEN: ""

  crunchyroll:
    command: "npx"
    args:
      - "-y"
      - "@striderlabs/mcp-crunchyroll"

  anilist:
    command: "npx"
    args:
      - "-y"
      - "mcp-server-anilist"

  postgres:
    command: "npx"
    args:
      - "-y"
      - "@modelcontextprotocol/server-postgres"
      - "postgresql://localhost/mydb"

  filesystem:
    command: "npx"
    args:
      - "-y"
      - "@modelcontextprotocol/server-filesystem"
      - "/var/home/howlcipher/dev"

  gitlab:
    command: "npx"
    args:
      - "-y"
      - "@modelcontextprotocol/server-gitlab"
    env:
      GITLAB_PERSONAL_ACCESS_TOKEN: ""

  kubernetes:
    command: "npx"
    args:
      - "-y"
      - "@modelcontextprotocol/server-kubernetes"

  docker:
    command: "npx"
    args:
      - "-y"
      - "@modelcontextprotocol/server-docker"

  yfinance:
    command: "uvx"
    args:
      - "mcp-server-yfinance"

  wikipedia:
    command: "npx"
    args:
      - "-y"
      - "@modelcontextprotocol/server-wikipedia"

  bravesearch:
    command: "npx"
    args:
      - "-y"
      - "@modelcontextprotocol/server-brave-search"
    env:
      BRAVE_API_KEY: "" # Add your Brave Search API key here or via .env

  memory:
    command: "npx"
    args:
      - "-y"
      - "@modelcontextprotocol/server-memory"

  sequentialthinking:
    command: "npx"
    args:
      - "-y"
      - "@modelcontextprotocol/server-sequential-thinking"

  puppeteer:
    command: "npx"
    args:
      - "-y"
      - "@modelcontextprotocol/server-puppeteer"

  gdrive:
    command: "npx"
    args:
      - "-y"
      - "@modelcontextprotocol/server-gdrive"

  jira:
    command: "npx"
    args:
      - "-y"
      - "@modelcontextprotocol/server-jira"
    env:
      JIRA_API_TOKEN: ""
      JIRA_EMAIL: ""
      JIRA_BASE_URL: ""

  linear:
    command: "npx"
    args:
      - "-y"
      - "@modelcontextprotocol/server-linear"
    env:
      LINEAR_API_KEY: ""
      
  trello:
    command: "npx"
    args:
      - "-y"
      - "@modelcontextprotocol/server-trello"
    env:
      TRELLO_API_KEY: ""
      TRELLO_TOKEN: ""

  sentry:
    command: "npx"
    args:
      - "-y"
      - "@modelcontextprotocol/server-sentry"
    env:
      SENTRY_AUTH_TOKEN: ""

  aws:
    command: "npx"
    args:
      - "-y"
      - "@modelcontextprotocol/server-aws"

  strava:
    command: "npx"
    args:
      - "-y"
      - "mcp-server-strava"

  steam:
    command: "npx"
    args:
      - "-y"
      - "mcp-server-steam"

  shodan:
    command: "npx"
    args:
      - "-y"
      - "mcp-server-shodan"
    env:
      SHODAN_API_KEY: ""

  baseball:
    command: "npx"
    args:
      - "-y"
      - "mcp-server-baseball"
