# 🚀 Improvement Backlog

This document is the authoritative, ranked backlog for the AI Knowledge Library. It is designed so a fresh session (Claude Code or Gemini CLI) can pick up the next item with zero prior chat context.

## Working Protocol

This protocol applies to every worked task — backlog improvements and bug fixes alike. Bugs are tracked in `issues.md`, which mirrors this file's format and shares this protocol; a hotfix without a backlog row still gets steps 1 through 4 and 7.

1. **Open a task journal.** Copy `documentation/task_journals/TEMPLATE.md` to `documentation/task_journals/YYYY-MM-DD_<slug>.md`. The journal is the resume point after a session limit or power outage: update it and commit at every milestone, and always keep its "Next step" line current so a fresh session can continue from the journal alone. It is a resume artifact, not permanent documentation — it gets deleted in step 7, so anything durable (findings, decisions, verification evidence) must land in the item's Done note, the changelog, or a proper doc before the task closes.
2. **Re-evaluate the model (every run, before starting).** The table's model columns are starting suggestions, not commitments. Check what is actually available right now — currently: Claude Pro subscription (Claude Code), Google subscription via Antigravity CLI (`agy models` lists live names; quota is per model), and local Ollama (`curl localhost:11434/api/tags` — models change). Update this sentence if subscriptions or tools change. Pick the least expensive available model that can do the job well and escalate only if the task proves harder than expected. To preserve Claude session limits, the default is to orchestrate from Claude Code but delegate implementation to a non-Claude model headlessly (`agy -p "<brief>" --model "<model>" --mode accept-edits`) per the delegation policy in `.agents/prompts/work_next_item.md`; Claude Code subagents bill the same Claude plan and do not save limits. Record the choice and one line of reasoning in the journal. Consult `documentation/model_tool_roster.md` for the maintained model roster and known delegation lessons before re-evaluating from scratch each time.
3. **Route the crafted skills.** Check the skills manifest (`.agents/skills.json` or the AGENTS.md table) for skills matching the task and read the matching SKILL.md files before planning. Honor tier precedence and the Related Skills deferrals.
4. **Scan for helpful free tools.** Briefly consider whether a free or open source tool (a CLI already installed, a linter, a library, an MCP server) would materially improve the work. Record recommendations in the journal; use tools that are already available freely, and ask before installing anything new. Check `documentation/model_tool_roster.md`'s Discovery sources section (There's An AI For That, the MCP server registry, package indexes) when nothing already available fits.
5. **Read the detail section** for the item (linked from the table) plus any referenced docs before coding. Background docs: `documentation/multi_agent_payload_protocol.md` and ADR 0003.
6. **Constraints:** this machine has no Anthropic or Gemini API keys; live LLM pipeline runs go through local Ollama.
7. **Finish the loop:** every code change ships with relevant automated tests in the same task — add or update tests covering the new behavior's success and failure paths (see `quality_assurance` for design standards). Use `make test-changed` for the fast loop while working; it also warns when a changed source file has no matching test, and that warning is a gap to fill, not noise. Verify the change works end to end, run the full `make test` before committing, commit with `<type>(<scope>): <description>`, set the item's Status to `Done (YYYY-MM-DD)` in the table, delete the task journal in the final commit (its durable record is the Done note and the changelog; a journal left behind means the task is unfinished), and push. Committing and pushing verified work is the default and needs no per-task approval; only destructive or history-rewriting git operations require asking first. This machine runs multiple agent sessions against the same clone concurrently, so `git status`/`git log` immediately before pushing may show commits this session did not make (e.g. `main` moved ahead while you worked). Do not force-clean or revert them — inspect the diff, and if it looks like ordinary in-progress work (not a secret, not a destructive change), push it along with your own commit rather than discarding it.

## Ranked Backlog (best ROI first)

Pending rows are ranked by a diminishing-returns score, recomputed at every groom (see `.agents/prompts/groom_backlogs.md`):

**Score = (Value × Decay) ÷ Effort**

- **Value (1–8):** pain or risk removed if the item ships.
- **Decay:** geometric halving per already-shipped item in the same theme (1.0 → 0.5 → 0.25 …) — the second polish pass on a theme is worth roughly half the first. New-capability items that open a new curve (e.g. #23, #24) always keep Decay 1.0; diminishing returns applies to polish, not to new ground.
- **Effort (1–8):** roughly log-scale; 1 = minutes, 8 = weeks.
- **ROI floor = 0.5:** items scoring below the floor stay open but are flagged ⚠️ and must not be worked without explicit user confirmation. At selection time the default is to skip past them to the highest-scoring above-floor item and ask the user whether to confirm, re-scope (shrink until it clears the floor), or close the flagged item.

Scores apply to Pending rows only; Done, Closed, and Merged rows show `—`.

| # | Improvement | Status | Score (V×D÷E) | Claude model | Gemini model | ROI rationale |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | [Rebuild the vector index](#1-rebuild-the-vector-index) | Done (2026-07-18) | — | Haiku 4.5 | Gemini 3 Flash | Minutes of work; unblocks semantic search over the newly refined skills |
| 2 | [Ignore build artifacts and local state in git](#2-ignore-build-artifacts-and-local-state-in-git) | Done (2026-07-18) | — | Haiku 4.5 | Gemini 3 Flash | Minutes of work; clears permanent noise from git status and prevents accidental commits |
| 3 | [Purge tracked scratch files from the repo root](#3-purge-tracked-scratch-files-from-the-repo-root) | Done (2026-07-18) | — | Haiku 4.5 | Gemini 3 Flash | Minutes of work; stops one-off scratch files traveling with every clone |
| 4 | [Make vector index rebuilds idempotent](#4-make-vector-index-rebuilds-idempotent) | Done (2026-07-18) | — | Haiku 4.5 | Gemini 3 Flash | Minutes of work; prevents stale chunks from silently corrupting every future rebuild |
| 5 | [Preflight the provider before a run](#5-preflight-the-provider-before-a-run) | Done (2026-07-18) | — | Sonnet 5 | Gemini 3 Flash | Small change; stops entire runs being wasted on a dead server or missing model tag |
| 6 | [Per tier LLM timeout](#6-per-tier-llm-timeout) | Done (2026-07-18) | — | Haiku 4.5 | Gemini 3 Flash | Config plumbing only; removes a known hard failure for 30B local models |
| 7 | [Separate transport failures from validation failures](#7-separate-transport-failures-from-validation-failures) | Done (2026-07-18) | — | Sonnet 5 | Gemini 3 Pro | Medium effort; makes every future failure diagnosable instead of masked |
| 8 | [Provider enforced structured outputs](#8-provider-enforced-structured-outputs) | Done (2026-07-18) | — | Sonnet 5 | Gemini 3 Pro | Medium effort; biggest single reliability win for small local models |
| 26 | [Dynamic test selection and tests-with-every-change protocol](#26-dynamic-test-selection-and-tests-with-every-change-protocol) | Done (2026-07-18) | — | Haiku 4.5 | Gemini 3 Flash | Small script plus protocol edit; every future item gets a fast relevant-test loop and guaranteed test coverage |
| 15 | [Fix the self-nesting docs site mirror](#15-fix-the-self-nesting-docs-site-mirror) | Done (2026-07-19) | — | Haiku 4.5 | Gemini 3 Flash | Small Makefile fix; stops the Pages mirror doubling itself on every `make docs` re-run and unblocks #27 |
| 34 | [Detect stranded worktree branches during item selection](#34-detect-stranded-worktree-branches-during-item-selection) | Done (2026-07-19) | — | Haiku 4.5 | Gemini 3 Flash | One-paragraph prompt edit; prevents a whole finished task being silently redone |
| 31 | [Stamp `content.sha256` in code at the gate](#31-stamp-contentsha256-in-code-at-the-gate) | Done (2026-07-19) | — | Haiku 4.5 | Gemini 3 Flash | Small gate change; removes the dominant remaining schema failure now that item 8 enforces the rest of the schema mechanically |
| 13 | [Install the pre-commit hook via bootstrap](#13-install-the-pre-commit-hook-via-bootstrap) | Done (2026-07-19) | — | Haiku 4.5 | Gemini 3 Flash | Small change; removes a silent per machine setup gap |
| 12 | [Emit gate failures into telemetry](#12-emit-gate-failures-into-telemetry) | Done (2026-07-19) | — | Haiku 4.5 | Gemini 3 Flash | Small change; starts tracking schema failure rate per model |
| 16 | [Pass the configured collection name to the vector store](#16-pass-the-configured-collection-name-to-the-vector-store) | Done (2026-07-19) | — | Haiku 4.5 | Gemini 3 Flash | Small change; stops `indexing.collection_name` config being silently ignored |
| 11 | [Guard against oversized local models](#11-guard-against-oversized-local-models) | Done (2026-07-19) | — | Haiku 4.5 | Gemini 3 Flash | Mostly covered by #7's attempt_errors; verify an OOM loop surfaces the Ollama error body, then close |
| 32 | [Structured JSON output for the web_research verifier](#32-structured-json-output-for-the-web_research-verifier) | Done (2026-07-19) | — | Haiku 4.5 | Gemini 3 Flash | Small change reusing item 8's pattern; ships standalone, decoupled from below-floor #33 |
| 36 | [Remove the dead `collection_name` constructor params](#36-remove-the-dead-collection_name-constructor-params) | Done (2026-07-19) | — | Haiku 4.5 | Gemini 3 Flash | Minutes of work; deletes a misleading no-op parameter before a future caller relies on it |
| 18 | [Free model and tool discovery protocol](#18-free-model-and-tool-discovery-protocol) | Done (2026-07-19) | — | Haiku 4.5 | Gemini 3 Flash | Small doc and protocol change; every future task can deliberately pull in extra free models and tools |
| 28 | [Audit and dynamize the GitHub Actions workflows](#28-audit-and-dynamize-the-github-actions-workflows) | Pending | 1.25 = 5×1.0÷4 | Sonnet 5 | Gemini 3 Pro | Workflow audit; cuts wasted CI minutes, drops dead workflows, and gates publishes on green tests |
| 27 | [Keep README, documentation, and the Pages site in sync on every commit](#27-keep-readme-documentation-and-the-pages-site-in-sync-on-every-commit) | Pending | 1.2 = 6×1.0÷5 | Haiku 4.5 | Gemini 3 Flash | Hook plus CI work; removes the whole class of hand-synced docs copies that drift (subsumes #14, builds on #15) |
| 10 | [Persist all attempt errors](#10-persist-all-attempt-errors) | Pending | 1.0 = 4×0.5÷2 | Haiku 4.5 | Gemini 3 Flash | Small change; preserves post mortem evidence currently thrown away and fixes `pipeline.attempt` fidelity (item 7 already captured the transport half) |
| 17 | [Make `PgVectorStore.upsert` a true upsert](#17-make-pgvectorstoreupsert-a-true-upsert) | Pending | 1.0 = 2×1.0÷2 | Haiku 4.5 | Gemini 3 Flash | Small change; removes the duplicate-row hazard for any future incremental pgvector use |
| 19 | [`pipeline_pass` frontmatter and dispatcher](#19-pipeline_pass-frontmatter-and-dispatcher) | Pending | 1.0 = 4×1.0÷4 | Sonnet 5 | Gemini 3 Pro | Medium effort; routes reviewer skills into the right pipeline pass |
| 20 | [Claude Code backend for pipeline tiers](#20-claude-code-backend-for-pipeline-tiers) | Pending | 1.0 = 4×1.0÷4 | Sonnet 5 | Gemini 3 Pro | Medium effort; adds a strong subscription backed judge for Tier 1 |
| 38 | [Detect concurrently active sessions before resuming in-flight work](#38-detect-concurrently-active-sessions-before-resuming-in-flight-work) | Pending | 1.0 = 4×0.5÷2 | Haiku 4.5 | Gemini 3 Flash | Small protocol addition; second instance of item 34's in-flight-detection theme — catches a live collision, not just a stranded one |
| 24 | [Automated job hunting pipeline](#24-automated-job-hunting-pipeline) | Pending | 0.88 = 7×1.0÷8 | Fable 5 | Gemini 3 Pro | High personal value; largest build, but a new curve — no decay applies |
| 23 | [Homelab MCP server](#23-homelab-mcp-server) | Pending | 0.86 = 6×1.0÷7 | Fable 5 | Gemini 3 Pro | High long term value; full architecture evaluation and build, but a new curve — no decay applies |
| 35 | [Untrack committed build/release artifacts in `dist/`](#35-untrack-committed-buildrelease-artifacts-in-dist) | Pending | 0.75 = 3×0.25÷1 | Haiku 4.5 | Gemini 3 Flash | Minutes of work; same tracked-before-ignored pattern as items 2/3 (third instance in the theme), 41MB of committed release binaries |
| 37 | [Fix pdoc output nondeterminism from set literal iteration order](#37-fix-pdoc-output-nondeterminism-from-set-literal-iteration-order) | Pending | 0.5 = 1×1.0÷2 | Haiku 4.5 | Gemini 3 Flash | At the ROI floor; cosmetic, gitignored output only, no functional impact — low urgency |
| 33 | [Harden the legacy loop and standalone LLM call sites](#33-harden-the-legacy-loop-and-standalone-llm-call-sites) | Pending ⚠️ below floor (confirmed 2026-07-19) | 0.42 = 5×0.25÷3 | Haiku 4.5 | Gemini 3 Flash | Third pass of the hardening theme; the target loop is actually the config default (`payload_pipeline.enabled: false`), not a rarely-used path (subsumes #25, #29, #30); user confirmed working it despite below-floor score |
| 9 | [Show exact object shapes in tier prompts](#9-show-exact-object-shapes-in-tier-prompts) | Closed (2026-07-18) | — | Sonnet 5 | Gemini 3 Flash | Superseded by #8's provider enforcement; shape examples would only serve the degraded fallback path |
| 22 | [OpenTelemetry integration](#22-opentelemetry-integration) | Closed (2026-07-19) | — | Sonnet 5 | Gemini 3 Pro | User closed at groom: no current outage better observability would have caught, effort not justified |
| 21 | [Calibrate the skill router score threshold](#21-calibrate-the-skill-router-score-threshold) | Closed (2026-07-19) | — | Fable 5 | Gemini 3 Pro | User closed at groom: shipped `triggers` frontmatter already captured most routing value; threshold tuning not worth a labeled eval set |
| 14 | [Sync the docs site changelog automatically](#14-sync-the-docs-site-changelog-automatically) | Merged into #27 (2026-07-18) | — | Haiku 4.5 | Gemini 3 Flash | Two line hook addition; stops docs/change_log.md drifting from the real changelog |
| 25 | [Preflight the provider in the legacy free-text loop](#25-preflight-the-provider-in-the-legacy-free-text-loop) | Merged into #33 (2026-07-18) | — | Haiku 4.5 | Gemini 3 Flash | Small change reusing item 5's module; the researcher/QA loop still burns iterations on a dead provider |
| 29 | [Propagate the LLM timeout to the remaining call sites](#29-propagate-the-llm-timeout-to-the-remaining-call-sites) | Merged into #33 (2026-07-18) | — | Haiku 4.5 | Gemini 3 Flash | Small change reusing item 6's plumbing; four other `litellm.completion` sites still hang on the 600s default |
| 30 | [Transport retry for the legacy loop and standalone call sites](#30-transport-retry-for-the-legacy-loop-and-standalone-call-sites) | Merged into #33 (2026-07-18) | — | Haiku 4.5 | Gemini 3 Flash | Small change reusing item 7's module; the legacy loop still burns iterations on transient provider blips |

## Details

### 1. Rebuild the vector index
`build_vector_index.py` was fixed to include dot directories (`.agents/skills/**` was invisible to the old glob), but the index has not been rebuilt since. The full skill refinement of 2026-07-18 (tiers, triggers, deduplicated content, Related Skills sections) is therefore invisible to semantic search. Run the rebuild so semantic search retrieves the refined skill content, and spot check a few queries (e.g. a security prompt should surface `cyber_security`).

**Done 2026-07-18:** Index rebuilt (219 chunks) with the `docs/` Pages mirror pruned from the scan — the first build indexed the mirror too (410 chunks) because the mirror paths contain the `.agents`/`documentation` substrings the file filter keeps, and duplicate copies crowded out results. Spot checks pass: security → `cyber_security`, crash triage → `defensive_debugging`, Terraform/K8s → `devops_sre`, resume → `career_assistant`. Findings spawned items 4 and 15.

### 2. Ignore build artifacts and local state in git
Several generated or local-state files are tracked or untracked-dirty and permanently clutter `git status`: `.telemetry/telemetry.db`, `build/lib/**`, `src/ai_knowledge_library.egg-info/**`, `__pycache__/` (including `scripts/__pycache__`), and the compiled Go binary `installer` (ELF executable rebuilt locally; track its source, not the binary). Add `.gitignore` entries, `git rm --cached` the already-tracked ones, and verify a fresh clone plus build still works. Check the existing installer-related `.gitignore` anchoring from commit `ce4309e` so the rules do not conflict.

**Done 2026-07-18:** Added `/build/`, `*.egg-info/`, and `.telemetry/` rules (installer and `__pycache__/` rules already existed from `ce4309e`; those files were simply committed before the rules landed) and untracked 60+ artifact files. Verified every path now matches an ignore rule via `git check-ignore -v`, and that the artifacts regenerate from source: `go build ./cmd/installer` and `pip install -e .` both succeed and leave `git status` clean. Findings spawned item 3 (tracked scratch files).

### 3. Purge tracked scratch files from the repo root
One-off working files are committed at the repo root and travel with every clone: `annotations.txt`, `coverage.out` (tracked before its ignore rule landed), `logs.zip`, `parsed.txt`, `patch.diff`, `test_312_output.log`, `test_312_sudo.log`, and `test_make.mk`. Decide per file whether it is evidence worth keeping (relocate under `documentation/` or `logs/`) or scratch (delete and `git rm`), then untrack the scratch set and add ignore rules for the recurring patterns (`*.log`, `*.diff`, `*.zip` at root, or specific names). Found during item 2, which fixed the generated-artifact half of the same problem. Related observation: the blanket `*.json` ignore rule silently hides `logs/payloads/**` pipeline run evidence from git; decide deliberately whether that should stay ignored or get a negation rule.

**Done 2026-07-18:** All eight files triaged as scratch and removed with `git rm` — none was evidence worth relocating (the CI failures they captured are fixed in history, `patch.diff`'s commit `07d2146` is merged on main, `logs.zip` was not even a valid archive, `test_312_sudo.log` was empty). Added root-anchored `/*.log`, `/*.diff`, `/*.zip` ignore rules; verified they match root probes only and hide no tracked file. Decision on `logs/payloads/**`: stays ignored — payload dirs are churny per-run LLM output, and the durable conclusions already live in `issues.md`, this backlog (items 5–12), and the backlog Done notes.

### 4. Make vector index rebuilds idempotent
`build_vector_index.py` only upserts; it never deletes existing chunks. Chunk ids are `<path>_<n>`, so when a file shrinks, moves, or is deleted, its leftover ids stay in the collection and every rebuild after content changes strands stale chunks (the 2026-07-18 rebuild required a manual `rm -rf .chroma` to purge the mirror duplicates). Drop and recreate the collection at the start of the build (or delete ids absent from the new scan) so a plain rerun always yields a clean index. Apply the same fix to the pgvector backend path.

**Done 2026-07-18:** Added an abstract `reset()` to `BaseVectorStore`; the Chroma backend drops the collection (recreated lazily by `upsert`) and the pgvector backend truncates the `documents` table (its `upsert` is a plain INSERT, so reruns duplicated every row). `build_vector_index.py` calls `reset()` after `init_db()`. Verified: two consecutive rebuilds hold a stable 230 chunks; a probe file indexed then deleted disappears from ids and query results on the next plain rerun; the security spot-check query still routes to `cyber_security`; all 98 tests pass. Findings spawned items 16 and 17.

### 5. Preflight the provider before a run
Run 1 (2026-07-17) spent all three validation attempts against a crashed Ollama server, and run 2 against a model tag that had been removed mid session. A cheap ping before pass 1 (list models, verify the configured tag exists, one token generation) would fail fast with an actionable error.

**Done 2026-07-18:** New `src/core/provider_preflight.py`, wired into `run_payload_loop` before pass 1 behind `payload_pipeline.preflight` (default on) with a per model `preflight_timeout` (default 120s, generous so a large local model can cold load — which also leaves it warm for pass 1). Each distinct tier model is checked once: Ollama models get a `/api/tags` reachability and tag-existence check (honoring `OLLAMA_API_BASE`, bare tags match `:latest`) before a one token generation; non-Ollama models go straight to the generation ping. All failures are collected, printed with actionable hints (`ollama serve`, available tags plus `ollama pull <tag>`, provider error body), and the run aborts returning `None` with zero validation attempts spent. Verified live against local Ollama: healthy tag passes (23s cold load), bogus tag lists the three available tags, dead server names the endpoint; 10 new unit tests, full suite 108 passing. Gate-focused tests in `test_validation_gate.py` now set `preflight: false` because their mocked `litellm.completion` side-effect lists are sized for exactly three passes. Findings spawned item 25 (the legacy free-text loop has the same failure mode).

### 6. Per tier LLM timeout
The 30B model timed out at LiteLLM's default 600s while generating the full payload envelope. Add a `timeout` setting to `payload_pipeline` (or per entry in `tier_models`) and pass it through `litellm.completion`.

**Done 2026-07-18:** `payload_pipeline.timeout` (default 600.0, LiteLLM parity) plus per tier `tier_timeouts.tier_1/2/3` overrides (0 falls back) in `settings.yaml` and `config_loader.py`. `Agent` gained an optional `timeout` passed to `litellm.completion` when set, and a shared `tier_setting()` helper now resolves both per tier models and per tier timeouts in `run_payload_loop`. Verified by 4 new tests (timeout kwarg reaches litellm, absent timeout omits the kwarg, fallback resolution, end-to-end per tier timeouts `[1800, 900, 900]` in pass order); full suite 120 Python + Go tests green. Findings spawned item 29 (four other `litellm.completion` call sites still run on the library default).

### 7. Separate transport failures from validation failures
`litellm.APIConnectionError` (server crash, model not found, timeout) surfaces as an empty response, which the gate scores as a `parse` failure and burns one of the three validation attempts. The persisted failed payload then reports `failure_vector: validation_gate.parse`, masking the real cause. Catch provider exceptions in the tier call path, retry them with backoff without consuming a validation attempt, and record a distinct error code (e.g. `UPSTREAM_UNAVAILABLE`) with the provider message in `error.context`.

**Done 2026-07-18:** New `src/core/transport_retry.py` (`call_with_transport_retry` with exponential backoff, `ProviderTransportError` carrying every attempt's provider message). `Agent.generate_response` gained `raise_errors` so the tier call path sees the real exception instead of `None`; the payload loop wraps each tier call in the retry (config: `payload_pipeline.transport_retries` default 2, `transport_backoff` default 2.0s) and on exhaustion persists a failed payload with `code: UPSTREAM_UNAVAILABLE`, `failure_vector: llm_transport.completion`, and the per attempt provider messages in `error.context.attempt_errors` — without spending any validation attempt. `ValidationGate.build_failed_payload` gained optional `code`/`failure_vector`/`context` overrides (defaults unchanged). Latent bug fixed en route: a telemetry logging failure used to discard a successful completion; telemetry errors now only warn. Verified by 12 new tests (retry backoff sequence, exhaustion error history, dead provider → 2 transport calls and zero gate attempts, transient blip → run completes with `attempt: 1`); full suite 130 Python + Go green. Findings spawned item 30 (legacy loop and standalone call sites lack the same retry) and extended item 10 (`pipeline.attempt` fidelity on non-gate failures).

### 8. Provider enforced structured outputs
`qwen3:30b-instruct` produced parseable JSON but invented fields on both real attempts (`lineage.history[0].action/details/timestamp`, then `critique.findings[0].description`). The VALIDATION ERROR feedback loop measurably improved attempt 2, but prompt discipline alone is weak on small local models. Ollama and LiteLLM support `response_format` with a JSON schema; passing `agent_task_payload.schema.json` (or a per pass subset) would make schema compliance mechanical instead of behavioral.

**Done 2026-07-18:** New `src/core/structured_output.py` builds a non-strict `json_schema` response format from `agent_task_payload.schema.json` (metadata keys `$schema`/`$id` stripped; strict mode off because the schema's optional fields are incompatible with OpenAI strict). `Agent` gained an optional `response_format` passed to `litellm.completion` (same pattern as `timeout`), wired into all three tier agents behind `payload_pipeline.structured_outputs` (default on, in `settings.yaml` and `config_loader.py`); a response-format build failure degrades gracefully to prompt-only discipline, and the gate still validates everything. Live probes proved Ollama accepts the full schema — including the if/then tier conditionals, regex patterns, and uuid/date-time formats — as its `format` decoding grammar, and litellm 1.83.0 passes it through. Live e2e through the production path (Agent + TIER3_PROMPT + real gate) on both `qwen2.5vl:7b` and the original offender `qwen3:30b-instruct`: output parses as bare JSON and passes schema validation with zero invented fields; the only remaining failure is the hash stage, which spawned item 31 (LLMs cannot compute SHA-256; stamp it in code). Also spawned item 32 (same enforcement for the web_research verifier). 9 new tests; full suite 139 green.

### 9. Show exact object shapes in tier prompts
The invented `lineage.history` fields suggest the model never saw the required entry shape. Embed a one line example of each nested object (history entry, finding, adversarial test) in the tier prompts, or echo the relevant schema fragment inside the VALIDATION ERROR feedback.

**Closed 2026-07-18 (groom):** superseded by item 8 — with `payload_pipeline.structured_outputs` on (the default), the provider constrains decoding to the schema, so invented or missing fields cannot occur on the enforced path; item 8's live 30B verification confirmed zero invented fields. Prompt shape examples would now only serve the degraded fallback (a response-format build failure or a provider without structured output support). Reopen only if such a provider enters the tier roster and shows schema misses.

### 10. Persist all attempt errors
The failed payload keeps only the final attempt's errors, so the schema violations from attempts 1 and 2 of run 3 are lost. Append a per attempt error history to `error.context` for post mortems. Also fix `pipeline.attempt` fidelity while there: `build_failed_payload` always stamps `attempt: max_attempts`, which is wrong for non-gate failures — a transport abort (item 7's `UPSTREAM_UNAVAILABLE` path) spends zero validation attempts yet the artifact claims all of them were used. Item 7 already persists transport attempt errors in `error.context.attempt_errors`; mirror that shape for gate attempts. Found during item 7.

**2026-07-19 groom (decay check):** item 11 (same broad diagnosability theme) closed today, but decay stays at 0.5, not 0.25 — item 11 shipped zero production code (it verified item 7's existing mechanism and added a regression test), so it added no new polish to this surface for the diminishing-returns model to discount. Score unchanged at 1.0.

**2026-07-19 groom (re-verified live):** still fully open. `ValidationGate.run()` (`src/core/validation_gate.py`) still calls `build_failed_payload(prev, last.errors, last.stage)` on exhaustion — only the final attempt's errors, no history — and `build_failed_payload` still unconditionally stamps `failed["pipeline"]["attempt"] = self.max_attempts` with no override path for that field, so the `UPSTREAM_UNAVAILABLE` transport-abort case in `orchestrator.py` (which overrides `code`/`failure_vector`/`context` but not this line) still reports `pipeline.attempt: max_attempts` despite spending zero gate attempts. One nuance found this groom: item 12 (done since this item was filed) now logs every failed gate attempt's model/pass/attempt/stage/error into the `gate_failures` SQLite table via `on_attempt_failure`, so per-attempt validation-gate history is no longer purely thrown away — it is queryable in telemetry, just not embedded in the payload artifact itself. This does not close the item (the artifact-embedded history and the `pipeline.attempt` fidelity bug are both still unfixed, and telemetry is a separate store from the persisted payload a post-mortem would actually open), but it modestly softens the "currently thrown away" framing. Value/Effort/score left unchanged (1.0) — the softening isn't large enough to justify a unilateral re-score.

### 11. Guard against oversized local models
`qwen3:30b-a3b` crashed the Ollama server mid load (29GB RAM host). When the configured model is served by a local endpoint, surface the provider's error body (Ollama returns JSON error messages) instead of the generic connection error, so OOM/crash loops are diagnosable from the pipeline log.

**2026-07-18 groom:** item 7 now covers most of this — every transport attempt's provider message is persisted verbatim (`ProviderTransportError.attempt_errors` → `error.context.attempt_errors`, printed on abort), and LiteLLM exception messages generally include the provider response body. Remaining scope has shrunk to verification: reproduce an Ollama OOM/crash loop, confirm the JSON error body actually appears in the persisted attempt errors, and only if it does not, add extraction of the response body at the transport-retry boundary. Close on a passing verification.

**Done 2026-07-19:** Re-scoped the verification method live. The model that originally OOM-crashed the host (`qwen3:30b-a3b`) is no longer pulled, and this host only has 23GB RAM available today — deliberately pulling a new oversized model just to re-crash the local Ollama server would need a new download (requires asking first per protocol) and risks destabilizing the host, so a literal re-crash was not attempted. Instead verified the underlying mechanism directly: a live `litellm.completion` call against a nonexistent Ollama tag raised `litellm.APIConnectionError: OllamaException - {"error":"model 'definitely-not-a-real-tag-xyz' not found"}` — confirming litellm embeds Ollama's raw `{"error": "..."}` JSON body verbatim in the exception's `str()`. Ollama uses this same error envelope for every server-side failure (404 not found, 500 OOM/crash alike), so this generically proves the mechanism for an OOM body too. Traced the capture path end to end: `call_with_transport_retry` (`src/core/transport_retry.py`) stores `f"{type(e).__name__}: {e}"` per attempt, and `orchestrator.py` persists it verbatim into the failed payload's `error.context.attempt_errors` on `ProviderTransportError` — confirmed intact, no gap. Added a regression test (`tests/test_transport_retry.py::test_ollama_oom_error_body_survives_verbatim_in_attempt_errors`) with a realistic Ollama OOM-shaped error body to lock in the finding without requiring a real crash. No implementation change needed — item 7 already fully covers this. Full suite 161 Python + 4 Go tests green.

### 12. Emit gate failures into telemetry
The validation gate prints failures but does not log them through `telemetry_logger`, so schema failure rate per model is not yet a tracked metric (recommended in `documentation/multi_agent_payload_protocol.md`).

**Done 2026-07-19:** `telemetry_logger.py` gained a `gate_failures` SQLite table plus `log_gate_failure(model, pass_number, attempt, stage, error_message)` and a matching `get_gate_failure_data()` reader, mirroring the existing `api_telemetry`/`log_telemetry`/`get_telemetry_data` pattern exactly. `ValidationGate.run()` gained an optional `on_attempt_failure(attempt, stage, errors)` callback, invoked once per failed attempt (including the final exhausting one) right after its existing `print`; passing nothing keeps prior behavior unchanged. `run_payload_loop` in `orchestrator.py` wires the callback to `log_gate_failure` with the current tier's `agent.model` and `pass_number`, wrapped in try/except so a telemetry write failure only warns and never aborts the run. No UI/dashboard added — out of scope per the source recommendation, which only asked for the metric to become trackable. 8 new tests (4 telemetry_logger, 4 validation_gate); full suite 156 Python + 4 Go tests green.

**Delegation note:** two delegate attempts failed before this was implemented directly. Gemini 3.5 Flash (Medium) via agy hit the shared account-wide quota immediately. GPT-OSS 120B via agy then ran for several minutes and returned a detailed, plausible "Changed files" table describing exactly the right edits — but made zero real changes anywhere on disk (confirmed via `git status`, `grep` for the new symbols, and a filesystem-wide mtime search including agy's own cache dirs). Unlike a quota error, this failure is silent and self-reports success. Promoted to the Antigravity delegation bullet in `work_next_item.md`: always verify a delegate's claimed diff with `git status`/`git diff` before trusting its summary.

### 13. Install the pre-commit hook via bootstrap
`.git/hooks` is not versioned; each machine needs a one time `python scripts/install_pre_commit_hook.py` (which now includes the skills manifest/index regeneration and the fixed `.env` guard). Wire it into `scripts/bootstrap.py` or the installer so it happens automatically. **2026-07-18 groom:** wire in `install_pre_push_hook.py` too, and route through the maintained installers only — `scripts/install_git_hooks.py`, a separate, unreferenced, obfuscated installer, was deleted 2026-07-19 (issues.md bug 1), so the maintained installers are the only ones left. **2026-07-19 groom:** `scripts/bootstrap.py` scaffolds a brand new *external* project directory (`.agents/skills` anchor file only) and has no path back to this repo's own `.git/hooks`, so it is not a real wiring target; the natural site is `cmd/installer`'s `Install()` method (`cmd/installer/main.go`), which already runs `scripts/install_global.sh`/`.ps1` as its last step and could add the two hook installers alongside it.

**Done 2026-07-19:** `Install()` now runs both `scripts/install_pre_commit_hook.py` and `scripts/install_pre_push_hook.py` (via a new `gitHookInstallerScripts` list, python3 with a python fallback, same pattern as the existing Google Docs setup step) right after the global-link step, guarded by a new `hasGitDir()` check that skips with an informational message when `.git` is not present. Implementation delegated to Antigravity CLI (GPT-OSS 120B — all three Gemini tiers were quota-exhausted at delegation time); reviewed and fixed one gap directly (`main_test.go` was missing the `os` import needed by its own new test). Verified: `go build`/`go vet` clean, both new Go unit tests pass (`hasGitDir` true/false across a temp dir, `gitHookInstallerScripts` contains exactly the two expected paths), manually re-ran `install_pre_push_hook.py` directly to confirm the underlying script still installs a working hook, full suite 143 Python + 4 Go tests green. Found during this task: `install_pre_push_hook.py` builds its hook filename via a `chr()` chain — the identical obfuscation pattern that got `install_git_hooks.py` deleted as issues.md bug 1 — filed as issues.md bug 2.

### 14. Sync the docs site changelog automatically
`docs/change_log.md` is a manual byte-for-byte copy of `change_log.md` for the GitHub Pages site and will drift as soon as someone updates the changelog without remembering the copy. Extend the pre-commit hook in `scripts/install_pre_commit_hook.py` to copy `change_log.md` to `docs/change_log.md` and stage it whenever the changelog is part of a commit (same pattern as the skills manifest regeneration). Consider whether `docs/docs.md` and `docs/index.md`, which duplicate README content, deserve the same treatment.

**Merged into #27 (2026-07-18):** the changelog copy is one instance of a general problem (committed generated copies that drift); item 27 solves the class instead of the instance. Work it there.

### 15. Fix the self-nesting docs site mirror
The `docs` target in the Makefile mirrors content with `cp -r documentation docs/` and `cp -r .agents docs/`. When the destination directory already exists, `cp -r` copies the source *into* it, so every `make docs` re-run without a prior `make clean` nests another copy: `docs/documentation/documentation/**` and `docs/.agents/.agents/**` exist in the repo today. Fix the target to remove (or `rsync --delete` into) the mirror subdirectories before copying, delete the committed nested trees, and confirm the Pages site still renders. Found during item 1, where the mirror also doubled the vector index (410 vs 219 chunks).

**Done 2026-07-19:** Re-evaluation corrected the mechanism: the current `cp -r documentation docs/` form *merges* on re-run (verified empirically) — the nesting came from the old recipe form `cp -r documentation docs/documentation` (commits `c97e3f2`, `7a6224a`), since replaced. The residue was real and worse than filed: 78 tracked nested files across **three** trees (`docs/documentation/documentation/**`, `docs/.agents/.agents/**`, and `docs/assets/assets/**`), redeployed to Pages on every push. Fix shipped: the `docs` recipe now starts with `rm -rf docs/api docs/documentation docs/assets docs/.agents` (also curing merge-without-delete stale-file stranding), the nested trees are deleted from git, and `tests/test_docs_mirror.py` guards both (no nested mirror paths tracked; recipe cleans before copying). Verified: two consecutive `make docs` runs yield byte-identical trees with zero nested paths; full suite 141 Python + Go green. Pages rendering is unaffected — `docs.yml` rebuilds the site from `make docs` on a fresh checkout, and nothing links into the nested paths. Verification also live-confirmed the committed mirror drift that motivates #27 (see its detail note).

### 16. Pass the configured collection name to the vector store
`VectorIndexBuilder` reads `indexing.collection_name` from `settings.yaml`, but `VectorStoreFactory.get_store()` constructs `ChromaVectorStore()` with its hardcoded default, so a custom collection name in config is silently ignored (everything happens to work today only because both sides fall back to the same default). Plumb the configured name through the factory into the backend constructors, and make the builder use the store's value rather than keeping a parallel copy. Found during item 4.

**Done 2026-07-19:** Re-evaluation found the root cause deeper than filed: `AppSettings` (`src/infrastructure/config_loader.py`) had no `indexing` field at all, and its `extra="ignore"` pydantic setting meant `ConfigLoader.get("indexing", {})` (and `load_config()`) silently dropped the entire `indexing:` block even if a user added one to `settings.yaml` — confirmed live before any fix by round-tripping a temp config file. This meant `collection_name`, `max_chunk_length`, and `batch_size` were all unconfigurable, not just the factory-to-backend link. Fix: added `IndexingSettings` (mirroring the existing `SkillRouterSettings` pattern) and an `indexing: IndexingSettings = IndexingSettings()` field on `AppSettings`; `VectorStoreFactory.get_store()` now reads `indexing.collection_name` from config and passes it to `ChromaVectorStore(collection_name=...)` (the `pgvector` branch is untouched — `PgVectorStore` has no collection concept, that's item 17's territory); removed `VectorIndexBuilder`'s now-fully-redundant duplicate `self.collection_name` field (confirmed dead before the fix too — assigned, never read elsewhere in the file). `src/ui/web_ui.py` and `src/core/web_research.py` automatically pick up the config-driven collection name for free since they already call the same factory with no arguments; their own unused `collection_name` constructor parameters are a separate pre-existing wart, left untouched. Implementation delegated to Antigravity CLI / GPT-OSS 120B (Gemini 3.5 Flash tiers hit the shared quota at delegation time); reviewed diff (only cosmetic double-blank-line cleanup needed), verified live end to end with a temp config file proving a custom `indexing.collection_name` reaches the constructed store's `.collection_name` attribute. 4 new tests in `tests/test_vector_store_factory.py` (config round-trip for a custom and default collection name, factory passes it to Chroma, factory leaves the pgvector branch args-free); full suite 160 Python + 4 Go tests green.

### 17. Make `PgVectorStore.upsert` a true upsert
`PgVectorStore.upsert` ignores the `ids` argument and issues a plain `INSERT` with no unique constraint, so it is not an upsert at all: any incremental (non-full-rebuild) use duplicates rows. Item 4's `reset()` masks this for full rebuilds only. Add a chunk id column with a unique constraint and use `INSERT ... ON CONFLICT DO UPDATE`, and store the chunk metadata alongside (`chunk` index is currently dropped on insert). Found during item 4.

### 18. Free model and tool discovery protocol
The Working Protocol's model re-evaluation (step 2) and free tool scan (step 4) currently rely on whatever the session already knows about. Build a maintained roster, e.g. `documentation/model_tool_roster.md`, of models and tools an agent may reach for beyond the defaults: local Ollama tags (queried live, they change), the Claude and Gemini subscriptions, free-tier hosted models, already-installed CLIs, and free MCP servers. Include discovery sources to consult when the roster has no fit — directories like There's An AI For That (theresanaiforthat.com), the MCP server registry, and package indexes — so a session can find a candidate rather than guess. Wire the roster into protocol steps 2 and 4. Hard rule to encode: anything free and already available may be used autonomously; anything paid, requiring signup or an API key, or needing a new install must be discussed with the user first. Requested 2026-07-18.

**2026-07-19 note:** several delegation lessons that belong in the eventual roster doc are currently scattered only in `work_next_item.md`'s Antigravity bullet (Gemini Flash/Pro tiers share one account-wide quota; GPT-OSS 120B fails independently; headless `agy` needs absolute paths and a Bash timeout above its own `--print-timeout`; backticked briefs must go through a file, not an inline string). When this item is worked, start by consolidating that bullet into the new roster doc rather than writing it from scratch.

**Done 2026-07-19:** New `documentation/model_tool_roster.md` with Models (Claude Pro/Claude Code, Google subscription via Antigravity CLI with the `agy models` invocation pattern, local Ollama), a Delegation lessons section consolidating the scattered `work_next_item.md` Antigravity bullet content (absolute-path requirement, the silent-fake-diff risk, the backtick/heredoc file-brief workaround, the shared Gemini-tier quota vs. independent GPT-OSS 120B quota, the Ollama fallback), a Discovery sources section (There's An AI For That, the MCP server registry, package indexes), and the verbatim hard rule on free-vs-paid autonomy. `improvements.md`'s Working Protocol steps 2 and 4 each gained one pointer sentence into the new doc. Implementation delegated to Antigravity CLI: Gemini 3.5 Flash (Medium) hit the shared account-wide quota immediately; GPT-OSS 120B (Medium) then produced both edits correctly on the first pass — reviewed and confirmed the diff was real and scoped to exactly the two intended files (no drift into the ranked table or other Working Protocol steps) — but rendered every compound-word hyphen as a U+2011 non-breaking hyphen throughout the new doc (14 instances: "limit-saving", "well-bounded", "self-reports", etc.), the same artifact class caught and fixed by hand in item 34's done note; fixed directly with a script substituting U+2011 back to plain ASCII `-`. Documentation-only change, no test surface; full suite 164 Python + 4 Go tests green.

### 19. `pipeline_pass` frontmatter and dispatcher
Skills now declare a priority `tier:` (0 meta, 1 governance, 2 domain, 3 application) consumed by `SkillRouter`, `skills.json`, and the AGENTS.md manifest. Pipeline pass affinity is a separate concept and still open: add a distinct key (e.g. `pipeline_pass: 2` for reviewer skills like `cyber_security`) so the payload pipeline dispatcher injects each skill into the pass where it belongs (reviewer skills into pass 2, not pass 1) without colliding with the priority tier semantics.

### 20. Claude Code backend for pipeline tiers
Add a `claude_code` option to `payload_pipeline.tier_models`, handled before LiteLLM by shelling out to headless Claude Code (`claude -p "<prompt>" --output-format json`). Uses the subscription login instead of an API key; bills session usage per run. Best fit: Tier 1 judging. Note this item can only be exercised on a machine with Claude Code logged in.

### 21. Calibrate the skill router score threshold
All 38 skills now declare `triggers` frontmatter, which closed the deterministic routing gap. The semantic fallback is still uncalibrated: build a small labeled prompt set and tune `skill_router.score_threshold` (and possibly `top_k`) against it so semantic recall is measured rather than guessed.

**2026-07-19 scoring:** below the ROI floor (0.25 = 2×0.5÷4) — the shipped `triggers` frontmatter already captured most of the routing value, and the eval-set build is comparatively expensive. Stays open per user decision, but requires explicit confirmation (or re-scoping) before being worked.

**Closed 2026-07-19 (user decision at groom):** user chose to close rather than confirm or re-scope. Reopen only if semantic-fallback misroutes are observed in practice — at that point a failing example gives the eval set a concrete seed instead of a speculative one.

### 22. OpenTelemetry integration
Integrate OpenTelemetry into the existing `system_logger.py` for advanced metrics and traces.

**2026-07-19 scoring:** below the ROI floor (0.4 = 2×1.0÷5) — sizable effort with no current outage it would have caught. Stays open per user decision, but requires explicit confirmation before being worked; re-score upward if an incident occurs that better observability would have shortened.

**Closed 2026-07-19 (user decision at groom):** user chose to close rather than confirm or re-scope. Reopen if an incident occurs that better observability would have shortened, per the re-score condition above.

### 23. Homelab MCP server
Build a homelab MCP server to autonomously monitor, debug, and manage local Docker containers and network infrastructure. Per the global rules, present a pros/cons evaluation of the technology options before committing to an architecture.

### 24. Automated job hunting pipeline
Script an automated pipeline using web-search MCPs to scrape job postings, map against `USER_PROFILE.md`, and generate tailored resumes and cover letters. Follow the `career_assistant` skill's grounding rules (no fabricated experience).

### 25. Preflight the provider in the legacy free-text loop
Item 5 added provider preflight to `run_payload_loop` only. The legacy researcher/QA loop (`run_loop` when `payload_pipeline.enabled` is false) has the same failure mode: a dead server or missing tag makes every `Agent.generate_response` return `None` and the graph burns its three iterations printing "Failed to get response". Reuse `src/core/provider_preflight.py` on `self.default_model` at the top of the non-payload `run_loop` path, honoring the same `payload_pipeline.preflight` toggle (or promote the setting out of `payload_pipeline` to a top level `preflight` section since it now guards both loops). Found during item 5.

**Merged into #33 (2026-07-18):** same code paths as items 29 and 30; one consolidated pass wires preflight, timeout, and transport retry together. Work it there.

### 26. Dynamic test selection and tests-with-every-change protocol
All existing verification runs the full suite (`make test`, the pre-push hook, CI); there is no fast loop that runs only the tests relevant to the current change, and the Working Protocol never required new tests with each worked item, so coverage grew ad hoc. Add `scripts/select_relevant_tests.py`: derive the change set from git (working tree vs `HEAD` plus untracked files, or `--base REF`), map changed Python sources to `tests/test_*.py` files that reference the module by name, include changed test files directly, fall back to the full suite when broad-surface files change (`Makefile`, `pyproject.toml`, `conftest.py`, `config/`, workflows, Go sources), and warn about changed sources with no matching test so coverage gaps are visible at selection time. Expose it as `make test-changed` and amend Working Protocol step 7 so every code change ships with relevant tests in the same task. Upgrade path if name matching proves too coarse: coverage-based selection via `pytest-testmon` (new install, needs approval per protocol step 4). Requested 2026-07-18.

**Done 2026-07-18:** `scripts/select_relevant_tests.py` implemented as designed, exposed as `make test-changed`, with 8 unit tests over the pure selection logic (`module_names`, `select_tests`). Verified live on its own change set: with the Makefile modified the selector correctly fell back to the full suite, and with that change stashed it mapped the new script to its test file while ignoring the journal markdown; full suite 116 Python + Go tests green. Working Protocol step 7 now requires relevant tests with every code change and points at `make test-changed`, treating an uncovered-source warning as a gap to fill. The docs mirror `docs/change_log.md` was synced manually — item 14 (automatic sync) remains the durable fix.

### 27. Keep README, documentation, and the Pages site in sync on every commit
Subsumes item 14. Today the docs site is a mix of generated-and-deployed content (`docs.yml` runs pdoc and deploys to Pages via `peaceiris/actions-gh-pages` on every push to main) and committed copies that drift silently: `docs/change_log.md` is a manual copy of `change_log.md` (item 26 needed a manual `cp`), `docs/docs.md` and `docs/index.md` duplicate README content, and `make docs` mirrors `documentation/` and `.agents/` into `docs/` (currently self-nesting, item 15). First investigation step: confirm what the Pages site actually serves — if `docs.yml` deploys a built artifact to the `gh-pages` branch, the committed `docs/` mirror may be dead weight that can stop being tracked entirely. **Recommended design (in preference order):** (1) stop committing generated copies — build the full site in `docs.yml` (README-derived index, changelog, `documentation/` and `.agents` mirrors, pdoc API docs) after fixing item 15, so main never carries a copy that can drift and every merge to main updates the live site automatically; (2) only where a copy must live in the repo, extend the pre-commit hook (`scripts/install_pre_commit_hook.py`) to regenerate and stage it when its source is part of the commit — the proven skills-manifest pattern — backed by a CI parity test like `test_skills_index.py` so drift fails the build; (3) treat the README as source of truth and generate the site landing page from it rather than keeping `docs/index.md` by hand. Depends on item 15, complements item 13 (hook installation via bootstrap). Requested 2026-07-18. **2026-07-19 (item 15 verification):** item 15 is done, unblocking this. The drift is live-confirmed and large: running `make docs` on a clean tree dirtied ~50 tracked mirror files (every `docs/.agents/skills/**` copy stale, `docs/index.md`, `docs/GEMINI.md`, `docs/_config.yml`) and surfaced untracked never-committed mirror content (`docs/AGENTS.md`, `docs/CLAUDE.md`, new skills, `docs/documentation/task_journals/`); that churn was deliberately reverted rather than committed, since this item decides whether the mirror stays tracked at all. Wrinkle to settle here: `make docs` mirrors `documentation/task_journals/` into the published site — decide whether in-flight journals belong on Pages. **2026-07-19 (item 13's push):** the drift is not just a manual-invocation risk — the installed pre-push hook runs `make test lint build docs`, so every ordinary `git push` regenerates the mirror against current source and dirties the tree the same way; this session's push had to be followed by a `git checkout -- docs/ && git clean -fd docs/` to discard the churn. Raises this item's practical urgency (it now fires on the default workflow, not an occasional command) without changing its filed Value/Effort. **2026-07-19 (issues.md bug 2 push):** same drift recurred on this push (`docs/AGENTS.md`, `docs/CLAUDE.md`, three new skill mirrors, task_journals mirror, README's 2026-07-13 cyberpunk rebrand never resynced). This time, rather than repeat the discard-and-reclean workaround, the drift was verified safe (all source content already committed, nothing in-progress) and committed as a one-off catch-up (`f1169f5`). That is a workaround, not a fix — the same manual judgment call (verify nothing in-progress is being clobbered, then decide commit vs. discard) will recur on every push until this item ships. **2026-07-19 (item 12's push):** a sharper failure mode than drift surfaced this time — a `git push` issued with a too-short Bash timeout killed the pre-push hook mid-`make docs`, leaving `docs/` with its mirrored subtrees deleted and not yet recopied (recovered by rerunning `make docs` to completion before retrying). Filed standalone as issues.md bug 3 (atomicity, not drift) since it can ship before this item lands; either item may absorb the other if this item ends up restructuring the `docs` recipe anyway. **2026-07-19 (groom, bug 3 shipped):** issues.md bug 3 is now done — the `docs` recipe builds into a `docs/.build-tmp` staging directory and swaps the four live subtrees into place, closing the interruption-safety gap standalone as anticipated. This item's remaining scope is unchanged and purely the drift problem described above (committed copies going stale between commits); the atomicity fix did not touch drift and did not restructure the recipe in a way that overlaps this item's design options.

**2026-07-19 (item 18's push):** drift recurred again, right on schedule — pushing item 18's commit regenerated `docs/documentation/model_tool_roster.md` (the new file this task added) as untracked mirror content, left uncommitted. This push also surfaced the "in-flight journals on Pages" wrinkle live for the first time: a concurrent session's uncommitted `documentation/task_journals/2026-07-19_william-elias-site-standout-features.md` got mirrored into `docs/documentation/task_journals/` by the same `make docs` run, purely because it existed untracked in the shared working tree at push time — confirming that any design for this item needs to either exclude `task_journals/` from the mirror outright or accept that in-progress, never-committed work from a concurrent session can appear on the live Pages site. Both untracked files were deliberately left alone rather than judgment-called into a catch-up commit this time, since one belonged to another session's still-open task. Value/Effort unchanged.

### 28. Audit and dynamize the GitHub Actions workflows
Nine workflows run today and none uses path filters, concurrency groups, or cross-workflow gating; every push to main triggers the full set, including docs-only commits. Audit findings to act on: `markdown_validation.yml` is a no-op (its only step is `echo Checking formatting rules...`, and it is written in JSON unlike every other workflow) — delete it or give it a real linter such as markdownlint; `docker-publish.yml` and `release_installer.yml` publish on every push to main rather than only on version tags, ungated by tests; `update_badges.yml` commits back to main on every push (bot-commit noise and a retrigger hazard); heavy jobs (`cross_platform.yml` OS matrix, CodeQL) run on commits that cannot affect them. **Recommended shape:** keep `test.yml` and `lint.yml` as the PR and merge gate; add `paths`/`paths-ignore` so docs-and-backlog-only commits skip code CI; add `concurrency` with `cancel-in-progress` to every workflow; chain expensive or publishing workflows on green tests — either `workflow_run` on `test.yml` success or a merged pipeline where publish jobs declare `needs: test` — which is the "dynamically with merges and tests complete" behavior; restrict `docker-publish.yml` and `release_installer.yml` to `v*` tags; keep CodeQL on its weekly schedule plus PRs; guard `update_badges.yml` with `paths-ignore` on its own output and `[skip ci]` on its commit. Re-verify each workflow still earns its keep before tuning it (cross_platform in particular: decide whether the three-OS install matrix still reflects how the installer is used). Coordinates with item 27, whose site build lands in `docs.yml`. Requested 2026-07-18.

### 29. Propagate the LLM timeout to the remaining call sites
Item 6 added a configurable timeout to the payload pipeline tier agents only. Four other `litellm.completion` call sites still hang on LiteLLM's 600s library default with no config control: the legacy free-text loop agents (Researcher/QA/Technical_Writer are built without a `timeout`, `src/core/orchestrator.py`), `src/core/adversarial_tester.py`, `src/core/web_research.py` (its HTTP scraper has a 15s timeout but its LLM verification call has none), and `src/infrastructure/semantic_search.py`. Reuse item 6's plumbing: pass the existing `Agent` timeout into the legacy loop agents and thread a configured timeout through the three standalone call sites — likely one top level `llm_timeout` setting rather than per module knobs, deciding then whether `payload_pipeline.timeout` should default to it. Coordinates with item 25, which touches the same legacy loop path. Found during item 6.

**Merged into #33 (2026-07-18):** same code paths as items 25 and 30; one consolidated pass wires preflight, timeout, and transport retry together. Work it there.

### 30. Transport retry for the legacy loop and standalone call sites
Item 7 added transport retry with backoff to the payload pipeline tier calls only. The legacy free-text loop agents (Researcher/QA/Technical_Writer) still call `Agent.generate_response` without `raise_errors`, so a transient provider blip returns `None` and burns one of the graph's three iterations (or silently skips humanization); the standalone `litellm.completion` sites in `src/core/adversarial_tester.py`, `src/core/web_research.py`, and `src/infrastructure/semantic_search.py` have no retry at all. Reuse `src/core/transport_retry.py` at those call sites. Coordinates with item 25 (legacy loop preflight) and item 29 (timeouts for the same sites) — consider working the three together since they touch the same code paths. Found during item 7.

**Merged into #33 (2026-07-18):** same code paths as items 25 and 29; one consolidated pass wires preflight, timeout, and transport retry together. Work it there.

### 31. Stamp `content.sha256` in code at the gate
Item 8's live 30B verification found the last remaining gate failure: LLMs cannot reliably compute a SHA-256 hash by hand, so `content.sha256` was the dominant cause of schema-valid-but-wrong payloads even with structured outputs enforced. Rather than keep asking the agent to attempt the hash, have `ValidationGate` recompute and stamp it in code on the passes that write `content.body`. Found during item 8.

**Done 2026-07-19:** `ValidationGate.check` now recomputes and overwrites `content.sha256` unconditionally on write passes (1 and 3) instead of trusting an LLM-computed hash, and still verifies the agent-supplied value on the read-only pass 2. Tier 3 and Tier 1 prompts (`src/core/tier_prompts.py`, `documentation/multi_agent_payload_protocol.md`) now tell agents to write a 64-zero placeholder rather than attempt SHA-256 by hand. Implementation delegated to Antigravity CLI (Gemini 3.1 Pro); reviewed and one stale module-docstring line fixed directly. Full suite green: 143 Python + 2 Go tests. `tests/test_tier_prompts.py` (direct coverage that the placeholder is schema-valid and agents are no longer asked to compute the hash) was written during this task but not staged in the original commit; committed separately in 804b41c.

### 32. Structured JSON output for the web_research verifier
`src/core/web_research.py`'s LLM verification call expects a `{verified, confidence, reason}` JSON object but relies on prompt discipline plus manual ` ```json ` code-fence stripping before `json.loads`. Reuse item 8's pattern: pass a small JSON schema (or at minimum `{"type": "json_object"}`) as `response_format` so the provider enforces the shape, and delete the fence-stripping fallback once verified. `src/core/adversarial_tester.py` was checked and is not a candidate (free-text keyword heuristic by design). Found during item 8.

**2026-07-19 decoupled from #33:** this item ships standalone in normal ranked order — it is above the ROI floor (1.5) while #33, which hardens the same call site with timeout and transport retry, is gated below it. Do not wait for or bundle #33; if #33 is later confirmed while this item is still open, #33 absorbs it, and if this item ships first, #33's scope simply shrinks by one file's worth of plumbing.

**Done 2026-07-19:** Added `verification_response_format()` to `src/core/structured_output.py` (same `json_schema` `response_format` shape as item 8's `payload_response_format`, reused as-is), and passed it as `response_format` in `ContentVerifier._llm_check`'s `litellm.completion` call in `src/core/web_research.py`. The verification schema (`{verified, confidence, reason}`) has no optional fields and sets `additionalProperties: false`, so unlike the payload schema it fully qualifies for OpenAI strict mode — set `strict: True`. Deliberate deviation from the item's literal text: the fence-stripping (` ```json `) plus `json.loads` fallback in `_llm_check` was **not** deleted. `resolve_utility_llm()` returns `(None, None)` on this machine (no `GEMINI_API_KEY`/`ANTHROPIC_API_KEY` configured), so the LLM verification path cannot be exercised live here — only via mocked unit tests — and this verifier's fallback chain includes providers (Gemini, Anthropic via litellm translation) whose `response_format` fidelity couldn't be live-confirmed. Kept the fallback as cheap defense-in-depth rather than risk a live parse failure with no way to catch it locally; a future session with a real provider key can delete it once genuinely verified. 4 new tests (2 `structured_output` shape/strict-mode, 1 `web_research` asserting `response_format` reaches `litellm.completion`, 1 pre-existing test unaffected); full suite 164 Python + 4 Go tests green. Implemented directly (no delegation) — small, well-bounded change reusing an existing pattern.

### 33. Harden the legacy loop and standalone LLM call sites
Subsumes items 25, 29, and 30 (groomed 2026-07-18), which each retrofitted one shipped payload-pipeline hardening onto the same non-pipeline code paths; working them separately would touch the same files three times. The paths: the legacy researcher/QA loop in `src/core/orchestrator.py` (`run_loop` when `payload_pipeline.enabled` is false) plus the standalone `litellm.completion` sites in `src/core/adversarial_tester.py`, `src/core/web_research.py`, and `src/infrastructure/semantic_search.py`. Scope, all by reuse of existing modules: (a) preflight — run `src/core/provider_preflight.py` on `self.default_model` at the top of the non-payload `run_loop`, deciding whether to promote `payload_pipeline.preflight` to a top-level `preflight` section since it would then guard both loops (ex-item 25); (b) timeout — thread a configured timeout through the legacy loop agents and the three standalone sites, likely one top-level `llm_timeout` setting, deciding whether `payload_pipeline.timeout` defaults to it (ex-item 29); (c) transport retry — wrap the same sites in `src/core/transport_retry.py`'s `call_with_transport_retry`, giving the legacy agents `raise_errors` so blips stop returning `None` (ex-item 30). Item 32 (structured output for the web_research verifier) touches the same file and is a natural companion in the same task. Original findings: 25 during item 5, 29 during item 6, 30 during item 7.

**2026-07-19 scoring:** below the ROI floor (0.42 = 5×0.25÷3) — this is the third application of the same hardening theme (items 5/6/7 shipped it on the pipeline path), onto the legacy loop that only runs when `payload_pipeline.enabled` is false. Stays open per user decision, but requires explicit confirmation before being worked; if confirmed while #32 is still open, fold #32 into the same task since it touches the same file (#32 is decoupled and ships standalone otherwise).

**2026-07-19 groom (framing correction):** re-verified against `config/settings.yaml` — `payload_pipeline.enabled: false` by default, and `--payload` is an opt-in CLI flag (`src/core/orchestrator.py`). This means `run_loop`'s free-text branch is the code path every unflagged run actually takes, not a rarely-exercised fallback; the item's own scope text and prior groom note mischaracterized it as "the legacy loop" in a dismissive sense. This does not mechanically change Decay (still third pass of the theme, 0.25) but is a material fact for the Value judgment the user is asked to make: hardening this item covers the default-active path, not an edge case. Left the Value/Effort numbers as filed (5/3) rather than unilaterally re-judging Value — surfaced to the user for the required below-floor confirmation instead of assumed.

**Confirmed 2026-07-19 (user decision at groom):** user confirmed working this item despite the below-floor score, on the strength of the framing correction above. The ⚠️ below-floor flag stays on the Status cell since the score math is unchanged (0.42, still under the 0.5 floor) — it is a transparency marker, not a work gate here — but a future `work_next_item` session may pick this item without re-asking for confirmation. If #32 is still open when this is picked up, fold it into the same task per the note above.

**2026-07-19 groom (item 32 shipped):** item 32 (structured output for the web_research verifier) is now done, shipped standalone as anticipated — this item's scope (preflight, timeout, transport retry across the legacy loop and the three standalone call sites) is unaffected, since 32 only touched `response_format` on the same file's LLM call, a different hardening dimension. The "fold #32 in if still open" contingency above no longer applies; nothing to fold.

### 34. Detect stranded worktree branches during item selection
While closing issues.md bug 1 (2026-07-19), the in-flight state was almost missed: a prior session had done the whole fix in `.claude/worktrees/bug1-remove-obfuscated-hook-installer/` — deletion commit, doc commit, branch pushed to origin — but its task journal was never committed, so it existed only inside the worktree, invisible to `work_next_item` step 1, which lists `documentation/task_journals/` in the main checkout. The stranded work was only noticed because `.claude/worktrees/` showed up untracked in `git status`. Fix: extend the Select step in `.agents/prompts/work_next_item.md` (and `resume_task`) to also check `git worktree list` and unmerged local or remote `worktree-*` branches for in-flight work before picking a new item, and to treat an uncommitted journal inside a worktree as a resume point. Consider also stating that finished worktree branches must be merged and removed before a session ends. Found during the bug 1 closure.

**Done 2026-07-19:** `work_next_item.md` Select gained a second bullet (check `git worktree list` and unmerged local/remote worktree branches; an uncommitted journal inside a worktree is a resume point; unmerged worktree commits may mean the item is already done and only needs merging and closing out), its Housekeeping bullet now requires finished worktree branches to be merged and their worktrees removed before session end, and `resume_task.md` step 1 performs the same check before declaring nothing in flight. Implementation delegated to GPT-OSS 120B via agy (all Gemini tiers were quota-exhausted with a shared ~27h reset, consistent with the shared-quota note); reviewed and touched up directly (list formatting, a U+2011 non-breaking hyphen, and the unmerged-commits-without-journal case the delegate dropped). Delegation lesson promoted into the Antigravity bullet: headless agy does not treat the invocation cwd as its workspace, so briefs must carry absolute file paths. Markdown-only change, no test surface; full suite 141 Python + Go green.

### 35. Untrack committed build/release artifacts in `dist/`
`dist/` is gitignored (the rule has existed since commit `ef5b8352`, 2026-07-08), but 18 files totaling ~41MB — a `.tar.gz` per OS/arch GoReleaser targets, `.deb`/`.rpm` packages, Windows `.exe` builds, and a checksums file — are still tracked, committed a day earlier in `696703b` (2026-07-07, the CI/lint/SAST/Makefile overhaul), before the ignore rule existed. Same root cause as items 2 and 3: files tracked before their ignore rule landed. `release_installer.yml` already runs `goreleaser release --clean --snapshot` on every push to main and the full release on `v*` tags, publishing these exact artifacts to GitHub Releases — nothing in the pipeline needs them committed to the working tree, and CI does not read from a pre-existing `dist/`. Fix: `git rm --cached` the tracked `dist/` files, verify `git status` stays clean after a local `goreleaser build --snapshot --clean` regenerates the directory, and confirm `release_installer.yml` still runs green (checkout does not depend on `dist/` pre-existing). Found during the 2026-07-19 groom while checking item 13's installer target — `git ls-files dist/` surfaced the tracked binaries.

### 36. Remove the dead `collection_name` constructor params
Found during item 16 (2026-07-19). `src/ui/web_ui.py`'s `KnowledgeUI.__init__` and `src/core/web_research.py`'s `VectorStoreManager.__init__` both accept a `collection_name: str = "ai_library_knowledge"` parameter, store it as `self.collection_name`, and then never pass it anywhere — both call `VectorStoreFactory.get_store()` with zero arguments, so the parameter has no effect. Confirmed no call site anywhere in `src/`, `tests/`, or `scripts/` ever passes a non-default value, so today this is inert, not a live bug — but it is a misleading API surface: a future caller reading the constructor signature would reasonably expect it to scope the search to that collection. Fix: delete the unused parameter and `self.collection_name` attribute from both classes (the factory already resolves the real collection name from `indexing.collection_name` config, per item 16).

**2026-07-19 groom (ranking bug fixed):** this row scored 2.0 (2×1.0÷1) since it was filed, the highest of any pending row, but had been left appended below lower-scoring rows (35 at 0.75, itself below 18/28/27/10/17/19/20/24/23 at 1.5 down to 0.88) instead of sorted into position — table order and score silently diverged. Re-sorted to the top of the pending block per the ranking rule. Score itself unchanged: no new same-theme item shipped since filing.

**Done 2026-07-19:** Re-verified live before implementing — no call site in `src/`, `tests/`, or `scripts/` passed a non-default `collection_name` to either constructor. Removed the `collection_name` parameter and `self.collection_name` attribute from `KnowledgeUI.__init__` (`src/ui/web_ui.py`) and `VectorStoreManager.__init__` (`src/core/web_research.py`); both already called `VectorStoreFactory.get_store()` with zero arguments, so behavior is unchanged. No test changes needed — existing tests already constructed both classes with defaults or `db_mode=` only. Applied directly (Claude Code, no delegation): effort was minutes, below the threshold where a delegate round-trip pays for itself. Full suite green: 164 Python + 4 Go tests.

### 37. Fix pdoc output nondeterminism from set literal iteration order
Found during issues.md bug 3 (2026-07-19) while verifying `make docs` idempotency. Two consecutive `make docs` runs are byte-identical for every tracked mirror path, but the gitignored `docs/api/` output differs run to run: `pdoc` renders the `repr()` of `src/core/skill_router.py`'s `FALLBACK_STOPWORDS` set literal directly into the generated HTML/JS (`docs/api/src/core/skill_router.html`, `docs/api/search.js`), and Python randomizes string hash seeds per process by default, so the set's iteration (and therefore its `repr()`) order varies between runs. Purely cosmetic — `docs/api/` is gitignored, never committed, and the set's actual membership/behavior is unaffected — but it means the Pages site's rendered API docs for that page churn on every rebuild for no functional reason, and it undermines the "byte-identical" idempotency guarantee item 15 and this task's bug 3 fix otherwise established for the rest of the mirror. Fix: change `FALLBACK_STOPWORDS` from a `set` literal to a `frozenset` over a sorted or otherwise stable-ordered sequence (e.g. `frozenset(sorted([...]))` at definition time, or define it as a sorted tuple and wrap in `frozenset()` for membership checks), so its `repr()` is deterministic regardless of `PYTHONHASHSEED`. Verify by running `make docs` twice and confirming `docs/api/src/core/skill_router.html` and `docs/api/search.js` are now byte-identical too.

### 38. Detect concurrently active sessions before resuming in-flight work
Found live during 2026-07-19 grooming: a fresh session resumed `documentation/task_journals/2026-07-19_william-elias-site-standout-features.md`'s "Next Step" (an ad hoc, non-backlog task in the sibling `william_elias` repo) at the exact moment another already-running Claude Code session was independently mid-delegation on a functionally overlapping feature in the same target files. Both sessions had picked up the same journal as their resume point. The current Select step (fixed by item 34) checks `git worktree list` and unmerged branches for *stranded* uncommitted work, but has no check for a *currently running* process already resuming the same journal — this collision was only caught because the session happened to run `ps aux` while investigating an unrelated unexpected `git status` diff, not because the protocol asked it to. It resolved safely only because the session paused (via `AskUserQuestion`) instead of proceeding blind, and the other session's own delegation had already finished cleanly by the time it re-checked. Fix: extend the Select step in `.agents/prompts/work_next_item.md` (and `resume_task.md`) to scan for other live `claude`/`agy` processes whose command line references the same journal path or target repo (e.g. `ps aux | grep -E "claude|agy"`, cross-referenced against the journal being resumed) before beginning delegation, and pause for user input if a match is found rather than assuming the field is clear. Same in-flight-detection theme as item 34 (second instance — Decay 0.5).

## ✅ Completed

- **Free model and tool discovery protocol (done 2026-07-19):** new `documentation/model_tool_roster.md` consolidates the model roster (Claude Pro, Antigravity CLI, local Ollama), scattered Antigravity delegation lessons, discovery sources, and the free-vs-paid autonomy rule; `improvements.md`'s Working Protocol steps 2 and 4 now point to it. Found during this task: GPT-OSS 120B via agy again produced the U+2011 non-breaking-hyphen artifact first caught in item 34 — worth watching for in future delegated prose.
- **Structured JSON output for the web_research verifier (done 2026-07-19):** `ContentVerifier._llm_check` now passes a `json_schema` `response_format` (new `verification_response_format()` in `src/core/structured_output.py`, reusing item 8's pattern) to `litellm.completion`, so providers that honor it constrain decoding to `{verified, confidence, reason}` mechanically. The fence-stripping/`json.loads` fallback was deliberately kept as defense-in-depth rather than deleted — this machine has no Gemini/Anthropic API key, so the LLM path could only be verified via mocked tests, not live against a real provider.
- **Guard against oversized local models (done 2026-07-19):** verified live that litellm embeds Ollama's raw `{"error": "..."}` JSON body verbatim in provider exceptions (probed via a nonexistent-tag 404; Ollama uses the same envelope for a 500 OOM/crash), and that `call_with_transport_retry` → `orchestrator.py` already carries it verbatim into the persisted failed payload's `error.context.attempt_errors` — item 7 fully covers this, no code change needed. Added a regression test locking in the finding. A real OOM re-crash was deliberately not attempted: the model that originally crashed the host is no longer pulled and doing so again would need a new download and risk destabilizing the host.
- **Emit gate failures into telemetry (done 2026-07-19):** `telemetry_logger.py` gained a `gate_failures` table and `log_gate_failure`/`get_gate_failure_data`, `ValidationGate.run()` gained an optional `on_attempt_failure` callback fired per failed attempt, and `orchestrator.py` wires it to log each tier's model, pass, attempt, stage, and error. Schema failure rate per model is now a queryable SQLite metric. Found during this task: GPT-OSS 120B via agy can return a fully plausible "changed files" summary while making zero real edits — a delegate's claimed diff must always be verified with `git status`/`git diff`, not trusted from its report.
- **Install the pre-commit hook via bootstrap (done 2026-07-19):** `cmd/installer`'s `Install()` now automatically runs both `install_pre_commit_hook.py` and `install_pre_push_hook.py` after the global-link step (skipped with a message when not inside a `.git` checkout), closing the silent per-machine setup gap. Found during this task: `install_pre_push_hook.py` uses the same `chr()`-chain obfuscation that got `install_git_hooks.py` deleted as bug 1 — filed as issues.md bug 2.
- **Stamp `content.sha256` in code at the gate (done 2026-07-19):** `ValidationGate.check` now recomputes and overwrites `content.sha256` unconditionally on write passes (1 and 3) instead of trusting an LLM-computed hash, and still verifies the agent-supplied value on the read-only pass 2. Tier 3 and Tier 1 prompts (`src/core/tier_prompts.py`, `documentation/multi_agent_payload_protocol.md`) now tell agents to write a 64-zero placeholder rather than attempt SHA-256 by hand. Implementation delegated to Antigravity CLI (Gemini 3.1 Pro); reviewed and one stale module-docstring line fixed directly. Full suite green: 143 Python + 2 Go tests.
- **Detect stranded worktree branches during item selection (done 2026-07-19):** the `work_next_item` Select step and `resume_task` step 1 now check `git worktree list` and unmerged worktree branches before picking or declaring nothing in flight, treating in-worktree journals and unmerged worktree commits as resume points; finished worktree branches must be merged and removed before a session ends. Also captured: headless agy delegates need absolute paths in their briefs.
- **Fix the self-nesting docs site mirror (done 2026-07-19):** the `docs` Makefile recipe now removes the mirror directories before copying, making `make docs` idempotent; 78 tracked nested residue files (`docs/documentation/documentation/**`, `docs/.agents/.agents/**`, `docs/assets/assets/**` — legacy of the old `cp -r src docs/src` recipe form) deleted, with `tests/test_docs_mirror.py` guarding against their return. Verified by byte-identical trees across consecutive runs. Unblocks item 27, whose detail note now carries the live-confirmed mirror-drift evidence.
- **Provider enforced structured outputs (done 2026-07-18):** the payload pipeline tier agents now hand the AgentTaskPayload schema to the provider via LiteLLM `response_format` (`src/core/structured_output.py`, `payload_pipeline.structured_outputs` default on), so Ollama constrains decoding to the schema and invented fields are impossible rather than merely caught. Live-verified on `qwen3:30b-instruct` (the model whose invented fields motivated the item): bare JSON, schema-valid, zero invented fields. Spawned item 31 (stamp `content.sha256` in code — the hash is now the dominant gate failure and models cannot compute it) and item 32 (same enforcement for the web_research verifier).
- **Separate transport failures from validation failures (done 2026-07-18):** provider exceptions in the payload pipeline tier calls are now retried with exponential backoff (`payload_pipeline.transport_retries`/`transport_backoff`) via `src/core/transport_retry.py`, and a provider that stays down aborts the pass with a persisted `UPSTREAM_UNAVAILABLE` failed payload (provider messages in `error.context.attempt_errors`) instead of burning validation attempts as fake `parse` failures. Also fixed: a telemetry logging failure no longer discards a successful completion. Spawned item 30 (same retry for the legacy loop and standalone call sites) and extended item 10 (`pipeline.attempt` fidelity).
- **Per tier LLM timeout (done 2026-07-18):** `payload_pipeline.timeout` plus `tier_timeouts` per tier overrides flow through the new `Agent` timeout into `litellm.completion`, so a slow 30B local drafting model can get a long tier 3 window while hosted judges stay tight. A shared `tier_setting()` helper resolves both per tier models and timeouts. Spawned item 29 (same timeout for the four remaining `litellm.completion` call sites).
- **Dynamic test selection and tests-with-every-change protocol (done 2026-07-18):** `scripts/select_relevant_tests.py` / `make test-changed` runs only the tests matching the current git change set, warns on changed sources with no matching test, and falls back to the full suite on broad-surface changes; Working Protocol step 7 now requires every code change to ship with relevant tests in the same task.
- **Preflight the provider before a run (done 2026-07-18):** `src/core/provider_preflight.py` checks every distinct tier model before pass 1 (Ollama server and tag via `/api/tags`, then a one token generation) and aborts with actionable hints instead of burning validation attempts on a dead provider. Config: `payload_pipeline.preflight` / `preflight_timeout`. Spawned item 25 (same guard for the legacy free-text loop).
- **Make vector index rebuilds idempotent (done 2026-07-18):** `BaseVectorStore` gained a `reset()` contract; the builder now empties the store (Chroma collection drop, pgvector table truncate) before upserting, so a plain rerun always yields a clean index with no manual `rm -rf .chroma`. Proven by a probe-file add/delete cycle and stable chunk counts across consecutive rebuilds. Spawned items 16 (collection name config ignored) and 17 (pgvector upsert is a plain INSERT).
- **Purge tracked scratch files from the repo root (done 2026-07-18):** eight one-off debugging files (`annotations.txt`, `coverage.out`, `logs.zip`, `parsed.txt`, `patch.diff`, two `test_312_*` logs, `test_make.mk`) removed after per-file triage confirmed none held unique evidence; root-anchored `/*.log`, `/*.diff`, `/*.zip` ignore rules prevent recurrence. `logs/payloads/**` deliberately stays ignored.
- **Ignore build artifacts and local state in git (done 2026-07-18):** `.gitignore` gained `/build/`, `*.egg-info/`, and `.telemetry/`; 60+ tracked artifacts (telemetry db, `build/lib/**`, installer binary, `__pycache__` caches, egg-info) untracked via `git rm --cached`. Go installer and editable pip install both rebuild cleanly with no new git noise. Spawned item 3 (tracked scratch files at the repo root).
- **Rebuild the vector index (done 2026-07-18):** fresh 219 chunk index over canonical sources only; `docs/` Pages mirror pruned from the scanner. Spot checks route security, debugging, IaC, and career prompts to the right skills. Spawned items 4 (idempotent rebuilds) and 15 (self-nesting docs mirror).
- **DevOps / IaC skill node (done 2026-07-17):** `.agents/skills/devops_sre/SKILL.md` exists; scope narrowed to Terraform/Kubernetes design during the 2026-07-18 skill refinement.
- **Skill routing recall gap for security prompts (done 2026-07-18):** all 38 skills declare `triggers` frontmatter; the failing prompt ("security hardening runbook for a FastAPI webhook server") now routes `cyber_security` deterministically. Threshold calibration continues as item 21.
- **Priority `tier` frontmatter (done 2026-07-18):** every SKILL.md declares `tier:` 0 to 3, parsed by `SkillRouter` and exposed in `skills.json` and the AGENTS.md manifest. See `documentation/skill_refinement_progress.md`. Pipeline pass affinity continues as item 19.
- **Skill library refinement (done 2026-07-18):** all 38 skills refined against a shared rubric, tiered, and cross deduplicated with Related Skills deferrals; tracked in `documentation/skill_refinement_progress.md`.
