{
  "name": "Markdown Validation",
  "on": {
    "push": {
      "branches": [
        "main"
      ]
    },
    "pull_request": {
      "branches": [
        "main"
      ]
    }
  },
  "jobs": {
    "validate_markdown": {
      "runs-on": "ubuntu-latest",
      "steps": [
        {
          "name": "Checkout code",
          "uses": "actions/checkout@v4"
        },
        {
          "name": "Check for punctuation rules",
          "run": "echo Checking formatting rules..."
        }
      ]
    }
  }
}
