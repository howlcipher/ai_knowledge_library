# Personal AI Knowledge Library

This repository serves as the central context layer for terminal agents. It maps directly into the CLI to enforce coding standards, eliminate hallucinations, and provide deep architectural context.

## Core Structure
* `GEMINI.md`: The global index and rulebook for the agent.
* `.agents/skills/`: Domain specific behavioral instructions.
* `scripts/`: Utilities for syncing and bootstrapping environments.\n